# Project Sync Bundle

Machine-authoritative declaration: `manifest.json`.

The `sync` operation is read-only by default. It compares canonical intended state with repository and optional new evidence, reports bounded proposals and affected planned work, and requires explicit current-envelope approval before any canonical/backlog apply pass.
