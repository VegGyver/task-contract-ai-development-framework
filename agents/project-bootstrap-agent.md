# Legacy Project Bootstrap Profile

Unregistered compatibility reference. Do not load this file for an operation.

Invoke:

```txt
tcaf bootstrap --target <target>
```

The registry resolves the canonical `agent-bundles/project-bootstrap/manifest.json` bundle.
