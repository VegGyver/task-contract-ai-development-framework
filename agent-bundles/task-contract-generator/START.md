# Start — Task Contract Generator

Use the Task Contract Generator Agent.

Provide one backlog item, issue, bug report or bounded request plus relevant project context.

Generate the shortest safe executable task contract. Do not implement. If the request is too broad, return decomposition or a blocked response using `OUTPUT-SCHEMA.md`.
