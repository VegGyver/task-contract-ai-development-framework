# Start — Project Sync

Inspect the bound target, canonical target-role evidence, repository evidence, and optional request/input evidence. Produce the concise review result declared by the output schema. Default to `WAITING FOR APPROVAL`; only an explicit, current-envelope, bounded approval may enter an apply pass. Stop on material conflict or uncertainty rather than modifying state.
