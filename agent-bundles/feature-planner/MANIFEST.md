# Feature Planner Bundle

Machine-authoritative declaration: `manifest.json`.

The `plan` operation prepares a read-only, canonical-first feature plan and
stops at developer approval. It does not implement `sync`, reconciliation or
application work.
