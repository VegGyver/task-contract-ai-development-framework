# Legacy Task Contract Generator Profile

Unregistered compatibility reference. Do not load this file for an operation.

Invoke:

```txt
tcaf task --target <target> --request <bounded request>
```

The registry resolves the canonical `agent-bundles/task-contract-generator/manifest.json` bundle.
