# Start — Project Bootstrap

Use the Project Bootstrap Agent.

Input the project goal, users, scope, constraints, known architecture decisions, tracker and naming preference.

Generate documentation only from canonical templates. Do not write application code. Do not invent missing decisions; use `To be decided` when non-blocking.

Return the generation report defined in `OUTPUT-SCHEMA.md` and stop for developer review.
