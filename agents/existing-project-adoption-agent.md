# Legacy Existing Project Adoption Profile

Unregistered compatibility reference. Do not load this file for an operation.

Invoke:

```txt
tcaf adopt --target <target>
```

The registry resolves the canonical `agent-bundles/existing-project-adoption/manifest.json` bundle.
